#include <iostream>
using namespace std;

int main() {
    double alpha, beta;

    cin >> alpha >> beta;

    if (alpha == 90 && beta == 90) {
        cout << "Prem " << endl;
    }
    else if (alpha == 90 || beta == 90) {
        cout << "Prem trap" << endl;
    }
    else if (alpha == beta) {
        cout << "ravno trap" << endl;
    }
    else {
        cout << "ne zna" << endl;
    }

    return 0;
}